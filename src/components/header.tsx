import * as React from 'react';

const Header: React.FC = () => {
  return (
    <header className="h-32 w-full flex flex-row-reverse">
      <button>Meny</button>
    </header>
  );
};

export default Header;
